{
  "models": [
    {
      "title": "Local Qwen Unsloth (UD-Q6)",
      "provider": "openai",
      "model": "${PRIMARY_MODEL}",
      "apiBase": "${LLAMA_SERVER_URL}",
      "apiKey": "vuoto",
      "contextLength": "${LLAMA_CONTEXT_LENGTH}",
      "completionOptions": {
        "temperature": 0.2,
        "topP": 0.9,
        "minP": 0.05,
        "repeatPenalty": 1.05
      },
      "systemMessage": "1. Max 150 lines per response.\n2. Before any file: list methods as stubs, ask 'Start with [method]?'.\n3. Never open a method you won't close.\n4. Respond with code only for the current task. No explanations."
    }
  ],
  "tabAutocompleteModel": {
    "title": "Local — Autocomplete",
    "provider": "openai",
    "model": "${AUTOCOMPLETE_MODEL:-none}",
    "apiBase": "${LLAMA_SERVER_URL}",
    "apiKey": "vuoto"
  },
  "embeddingsProvider": {
    "provider": "transformers.js"
  },
  "contextProviders": [
    {
      "name": "codebase",
      "params": {}
    }
  ],
  "systemMessage": "Max 150 lines. Ask before starting a method. Respond with code only.",
  "chatOptions": {
    "baseSystemMessage": "Max 150 lines. Ask before starting a method. Respond with code only."
  },
  "allowAnonymousTelemetry": false
}